[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oanim-core"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
bindings = "pyo3"
features = ["pyo3/abi3-py38"]
module-name = "oanim_core"
strip = true
